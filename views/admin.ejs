<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة التحكم</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>body { font-family: 'Cairo', sans-serif; }</style>
</head>
<body class="bg-gray-100 pb-10">

    <nav class="bg-slate-900 text-white p-4 shadow-md mb-8">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-xl font-bold">لوحة تحكم الهندسية</h1>
            <a href="/" class="text-sm bg-gray-700 px-3 py-1 rounded hover:bg-gray-600">عرض الموقع</a>
        </div>
    </nav>

    <div class="container mx-auto px-4">
        
        <div class="bg-white rounded-xl shadow-lg p-6 mb-10 max-w-2xl mx-auto border-t-4 border-amber-500">
            <h2 class="text-2xl font-bold mb-6 text-slate-800 flex items-center gap-2">
                <i class="fas fa-plus-circle text-amber-500"></i> إضافة عمل جديد
            </h2>
            <form action="/add-project" method="POST" enctype="multipart/form-data" class="space-y-4">
                <div>
                    <label class="block text-gray-700 font-bold mb-1">عنوان العمل</label>
                    <input type="text" name="title" class="w-full p-2 border rounded focus:ring-2 focus:ring-amber-500 outline-none" required>
                </div>
                <div>
                    <label class="block text-gray-700 font-bold mb-1">القسم</label>
                    <select name="category" class="w-full p-2 border rounded focus:ring-2 focus:ring-amber-500 outline-none">
                        <option value="مطبخ">مطبخ</option>
                        <option value="شباك">شباك</option>
                        <option value="باب">باب</option>
                    </select>
                </div>
                <div>
                    <label class="block text-gray-700 font-bold mb-1">الوصف</label>
                    <textarea name="description" rows="3" class="w-full p-2 border rounded focus:ring-2 focus:ring-amber-500 outline-none"></textarea>
                </div>
                <div>
                    <label class="block text-gray-700 font-bold mb-1">الصور</label>
                    <input type="file" name="photos" multiple class="w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:bg-amber-100 file:text-amber-700 hover:file:bg-amber-200" required>
                </div>
                <button type="submit" class="w-full bg-slate-900 text-white font-bold py-3 rounded hover:bg-slate-800 transition">نشر</button>
            </form>
        </div>

        <div class="bg-white rounded-xl shadow-lg p-6">
            <h2 class="text-2xl font-bold mb-6 text-slate-800 flex items-center gap-2">
                <i class="fas fa-tasks text-blue-500"></i> إدارة الأعمال الحالية
            </h2>
            
            <div class="overflow-x-auto">
                <table class="w-full text-right border-collapse">
                    <thead>
                        <tr class="bg-gray-50 border-b">
                            <th class="p-3">الصورة</th>
                            <th class="p-3">العنوان</th>
                            <th class="p-3">القسم</th>
                            <th class="p-3">إجراءات</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% projects.forEach(function(project) { %>
                            <tr class="border-b hover:bg-gray-50">
                                <td class="p-3">
                                    <img src="<%= project.mainimage %>" class="w-16 h-16 object-cover rounded">
                                </td>
                                <td class="p-3 font-bold"><%= project.title %></td>
                                <td class="p-3"><span class="bg-gray-200 text-xs px-2 py-1 rounded"><%= project.category %></span></td>
                                <td class="p-3 flex gap-2">
                                    <a href="/edit-project/<%= project.id %>" class="bg-blue-500 text-white px-3 py-1 rounded hover:bg-blue-600 text-sm">
                                        <i class="fas fa-edit"></i> تعديل
                                    </a>
                                    <form action="/delete-project/<%= project.id %>" method="POST" onsubmit="return confirm('هل أنت متأكد من حذف هذا العمل؟');">
                                        <button type="submit" class="bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600 text-sm">
                                            <i class="fas fa-trash"></i> حذف
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</body>
</html>