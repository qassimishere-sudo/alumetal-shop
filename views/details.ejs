<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title><%= product.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Cairo', sans-serif; }</style>
</head>
<body class="bg-gray-50">

    <nav class="bg-slate-900 text-white p-4">
        <div class="max-w-7xl mx-auto flex justify-between items-center">
            <a href="/" class="flex items-center gap-2 hover:text-amber-500">
                <span>&larr;</span> عودة للمعرض
            </a>
            <span class="font-bold text-xl"><%= product.title %></span>
        </div>
    </nav>

    <div class="max-w-6xl mx-auto p-6">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <div class="lg:col-span-2 space-y-4">
                <img id="mainImg" src="<%= product.imagesList[0] %>" class="w-full h-[500px] object-cover rounded-xl shadow-lg">
                <div class="flex gap-2 overflow-x-auto pb-2">
                    <% product.imagesList.forEach(function(img) { %>
                        <img src="<%= img %>" 
                             class="w-24 h-24 object-cover rounded-lg cursor-pointer border-2 border-transparent hover:border-amber-500 transition-all"
                             onclick="document.getElementById('mainImg').src=this.src">
                    <% }); %>
                </div>
            </div>

            <div class="bg-white p-8 rounded-xl shadow-lg h-fit">
                <span class="bg-amber-100 text-amber-800 px-3 py-1 rounded-full text-sm font-semibold mb-4 inline-block">
                    <%= product.category %>
                </span>
                <h1 class="text-3xl font-bold text-slate-900 mb-4"><%= product.title %></h1>
                <p class="text-gray-600 leading-relaxed mb-8"><%= product.description %></p>
                
                <a href="https://wa.me/201000000000" class="block w-full bg-green-500 hover:bg-green-600 text-white text-center font-bold py-4 rounded-xl transition-colors shadow-lg">
                    اطلب هذا التصميم واتساب
                </a>
            </div>
        </div>
    </div>

</body>
</html>